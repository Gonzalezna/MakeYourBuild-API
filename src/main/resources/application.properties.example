# MakeYourBuild API - Archivo de Configuración de Ejemplo
# 
# INSTRUCCIONES:
# 1. Copia este archivo como 'application.properties'
# 2. Reemplaza los valores de ejemplo con tus credenciales reales

spring.application.name=MakeYourBuild API

# Configuración del servidor
server.port=8080
server.servlet.context-path=/

# Configuración de CORS (desarrollo)
# En producción, configura los orígenes permitidos según tu dominio
cors.allowed-origins=http://localhost:3000,http://localhost:5173,http://localhost:4200

# Configuración de la base de datos (Supabase PostgreSQL)
# IMPORTANTE: Reemplaza estos valores con tus credenciales reales
spring.datasource.url=jdbc:postgresql://TU_HOST:5432/postgres?sslmode=require
spring.datasource.username=TU_USUARIO
spring.datasource.password=TU_CONTRASEÑA
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate
# En desarrollo: update (crea/modifica tablas automáticamente)
# En producción: validate (solo valida, no modifica esquema)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=false

# Logging
# En desarrollo: DEBUG (muestra todo)
# En producción: INFO o WARN (solo eventos importantes)
# IMPORTANTE: Nunca loguear passwords, tokens o información sensible
logging.level.com.makeyourbuild.api=INFO
logging.level.org.springframework.security=WARN
logging.level.org.hibernate.SQL=false
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=false
